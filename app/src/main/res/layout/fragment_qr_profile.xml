<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/home_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ufps_blanco_favorito">

    <!-- AppBar fija arriba -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/LayoutAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:minHeight="72dp"
        android:background="@color/ufps_gris_principal"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/topAppBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="?attr/actionBarSize"
            app:title="Tu código QR"
            app:titleCentered="true"
            app:titleTextAppearance="@style/TextTitle"
            app:navigationIcon="@drawable/arrow_back_black_icon" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- CONTENIDO SCROLLEABLE -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/qrScroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/LayoutAppBar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Contenedor del contenido del scroll: ahora ConstraintLayout -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/qr_content_root"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- TARJETA DEL QR -->
            <androidx.cardview.widget.CardView
                android:id="@+id/cardQR"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="24dp"
                android:layout_marginEnd="24dp"
                android:layout_marginTop="24dp"
                android:paddingTop="24dp"
                android:paddingBottom="12dp"
                app:cardCornerRadius="35dp"
                app:cardElevation="4dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toTopOf="@id/backgroundLogo">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:gravity="center">

                    <!-- Avatar del usuario -->
                    <ImageView
                        android:id="@+id/imgProfile"
                        android:layout_width="90dp"
                        android:layout_height="90dp"
                        android:layout_marginTop="15dp"
                        android:layout_marginBottom="4dp"
                        android:background="@drawable/rounded_image"
                        android:clipToOutline="true"
                        android:scaleType="centerCrop"
                        android:src="@drawable/no_image" />

                    <!-- Nombre del usuario -->
                    <TextView
                        style="@style/TextTitle"
                        android:id="@+id/txtUserName"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Juan García"
                        android:layout_marginBottom="2dp" />

                    <!-- Timer del QR -->
                    <LinearLayout
                        android:id="@+id/layoutTimer"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center"
                        android:layout_marginBottom="6dp"
                        android:background="@drawable/timer_background"
                        android:padding="8dp">

                        <ImageView
                            android:layout_width="20dp"
                            android:layout_height="20dp"
                            android:src="@drawable/ic_timer"
                            android:layout_marginEnd="6dp"
                            app:tint="@color/ufps_principal" />

                        <TextView
                            android:id="@+id/txtQRTimer"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="59:30"
                            android:textSize="16sp"
                            android:fontFamily="@font/poppins_semibold"
                            android:textColor="@color/ufps_texto_oscuro" />
                    </LinearLayout>

                    <!-- Imagen del QR -->
                    <ImageView
                        android:id="@+id/imgQr"
                        android:layout_width="220dp"
                        android:layout_height="220dp"
                        android:layout_marginBottom="8dp"
                        android:contentDescription="Código QR" />

                    <!-- Badge de estado -->
                    <TextView
                        android:id="@+id/txtQRStatus"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="✓ Código válido"
                        android:textSize="14sp"
                        android:fontFamily="@font/poppins_medium"
                        android:textColor="@color/ufps_success_principal"
                        android:layout_marginBottom="4dp"
                        android:drawablePadding="4dp" />

                    <!-- Texto informativo -->
                    <TextView
                        style="@style/DefaultText"
                        android:id="@+id/txtInfo"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:fontFamily="@font/poppins_italic"
                        android:text="@string/text_share_qr_profile"
                        android:textColor="@color/ufps_texto_oscuro"
                        android:layout_marginBottom="4dp" />

                    <!-- Marca -->
                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center"
                        android:layout_marginBottom="15dp">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/text_label_home_screen_2"
                            android:textColor="@android:color/black"
                            android:textSize="20sp"
                            android:fontFamily="@font/poppins_semibold"
                            android:includeFontPadding="false" />

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/text_label_home_screen_3"
                            android:textColor="@color/ufps_principal"
                            android:textSize="20sp"
                            android:textStyle="bold"
                            android:fontFamily="@font/poppins_semibold_italic"
                            android:includeFontPadding="false" />
                    </LinearLayout>

                </LinearLayout>

            </androidx.cardview.widget.CardView>

            <!-- Logo anclado al fondo -->
            <ImageView
                android:id="@+id/backgroundLogo"
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:src="@drawable/icon_recable_logo"
                android:scaleType="centerInside"
                android:layout_marginBottom="15dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.core.widget.NestedScrollView>

    <!-- Overlay de carga encima de todo -->
    <include
        layout="@layout/loading_overlay" />

</androidx.constraintlayout.widget.ConstraintLayout>
